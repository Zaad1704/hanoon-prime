[pytest]
testpaths = tests
pythonpath = src
addopts = -v --strict-markers --tb=short --maxfail=5
markers =
    contract: architectural contract tests (always run, can't be skipped)
    backtest: backtest/profitability tests (slow, run in CI)
    unit: unit tests
    live: tests requiring IB connection (skipped by default)
